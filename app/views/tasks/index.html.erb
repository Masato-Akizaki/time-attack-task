<%= provide(:left_icon, image_tag("menu_icon")) %> 
<%= provide(:nav_title, "TimeAttackTask")%>
<%= provide(:right_icon, (link_to "📁", projects_url, id: "projects_url"))%>
<div class="container">
  <% @tasks.each do |task| %>
    <div class="card <%= "done" if task.status == 1%>">
      <p class="task-name"><%= link_to(task.name, edit_task_path(task))  %></p>
      <% if false %>
        <% if task.memo.present? %>
          <%= simple_format(task.memo, class: "task-memo") %>
        <% end %>
        <% if task.date.present? %>
          <p class="task-date"><%= task.date %></p>
        <% end %>
        <% if task.time.present? %>
          <p class="task-time"><%= task.time %>min</p>
        <% end %>
      <% end %>
    </div>
  <% end %>
</div>

<%= link_to image_tag('new_task.png'), new_task_path, class: "new-task-icon" %>