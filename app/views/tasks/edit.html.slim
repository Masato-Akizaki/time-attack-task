= provide(:left_icon, (link_to image_tag("back_icon"), tasks_url, id: "back_icon"))
= provide(:nav_title, "タスクを編集")
= render "task_form", button_text: '保存', task: @task, class: 'btn'
= link_to '削除', @task, class: 'btn btn-delete', method: :delete, data: { confirm: 'Are you sure?' }
